server:
    port: 8081

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8000/eureka
    instance:
        instance-id: springcloud-config-client

management:
    endpoints:
        web:
            exposure:
                include: "*"
    endpoint:
        health:
            enabled: true